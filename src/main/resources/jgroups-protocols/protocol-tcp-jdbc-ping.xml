<!-- TCP based stack, with flow control and message bundling. This is usually
used when IP multicasting cannot be used in a network, e.g. because it is
disabled (routers discard multicast). -->
<config xmlns="urn:org:jgroups" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
		xsi:schemaLocation="urn:org:jgroups http://www.jgroups.org/schema/jgroups.xsd">
	<TCP
			bind_port="${activeviam.distribution.tcp.bind_port:16484}"
			bind_addr="${activeviam.distribution.tcp.bind_address:127.0.0.1}"
			port_range="200"
			recv_buf_size="20M"
			send_buf_size="20M"
			sock_conn_timeout="300"
			thread_pool.min_threads="0"
			thread_pool.max_threads="200"
			thread_pool.keep_alive_time="30000" />
	<JDBC_PING
			connection_url="${JDBC_PING_URL}"
			connection_username="${JDBC_PING_USERNAME:user}"
			connection_password="${JDBC_PING_PASSWORD:password}"
			connection_driver="${JDBC_PING_DRIVER:org.postgresql.Driver}"
			remove_all_data_on_view_change="true"
			remove_old_coords_on_view_change="true"
			write_data_on_find="true"
			initialize_sql="${JDBC_PING_INIT_SQL:CREATE TABLE IF NOT EXISTS JGROUPSPING (own_addr varchar(200) NOT NULL,bind_addr varchar(200) NOT NULL,created timestamp NOT NULL,cluster_name varchar(200) NOT NULL,ping_data BYTEA,constraint PK_JGROUPSPING PRIMARY KEY (own_addr, cluster_name))}"
			insert_single_sql="INSERT INTO JGROUPSPING (own_addr, bind_addr, created, cluster_name, ping_data) values (?,'${activeviam.distribution.tcp.bind_address:127.0.0.1}',NOW(), ?, ?)"
			delete_single_sql="DELETE FROM JGROUPSPING WHERE own_addr=? AND cluster_name=?"
			select_all_pingdata_sql="SELECT ping_data FROM JGROUPSPING WHERE cluster_name=?"
	/>
	<MERGE3 min_interval="10000" max_interval="30000" />
	<FD_SOCK get_cache_timeout="10000"
			 cache_max_elements="300"
			 cache_max_age="60000"
			 suspect_msg_interval="10000"
			 num_tries="8"
			 sock_conn_timeout="50000"/>
	<FD_ALL timeout="12000" interval="3000" timeout_check_interval="2000"/>
	<VERIFY_SUSPECT timeout="30000" num_msgs="10"/>
	<BARRIER />
	<pbcast.NAKACK2
			max_rebroadcast_timeout="5000"
			use_mcast_xmit="false"
			discard_delivered_msgs="true" />
	<UNICAST3 />
	<pbcast.STABLE
			desired_avg_gossip="50000"
			max_bytes="4M" />
	<AUTH
		auth_class="com.activeviam.activepivot.dist.impl.internal.distribution.security.impl.AtotiAuthToken"
		auth_token.auth_value="distribution_password"
		auth_token.token_hash="SHA" />
	<pbcast.GMS
			print_local_addr="true"
			join_timeout="30000"
			leave_timeout="30000"
			merge_timeout="30000"
			num_prev_mbrs="200"
			view_ack_collection_timeout="30000"/>
</config>